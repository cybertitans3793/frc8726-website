name: Deploy to OrangePi5

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy webhook
        run: |
          curl -X POST https://deploy.frc8726.org/hooks/deploy-website
